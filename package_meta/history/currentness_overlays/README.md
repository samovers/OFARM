# Currentness Overlay History

This folder preserves pre-Phase 5 layered currentness prose.

Status:

- Historical lineage only.
- Not active OFARM law.
- Not the default development reading path.
- Does not override current root entrypoints.
- Does not override `00_active_baseline/`.
- Does not promote draft or non-default contracts.

Old package identities inside this folder are historical/provenance references only.

The Phase 5 pre-rewrite snapshots live in `package_meta/history/currentness_overlays/phase5_pre_rewrite/`.


# OFARM Golden Path Application Demo v0.1

This demo packages the normal Field 7 workflow as an app-builder fixture:

```text
field setup → observation → advisory recommendation → prescription → work order → operation claim/evidence → accepted execution → current-state view → PassportView preview
```

It is not a platform implementation. It is a conformance and SDK-example target for AI coding agents.

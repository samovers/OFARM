
# OFARM Hard Path Application Demo v0.1

This demo packages the workflow most likely to break AI-generated apps:

```text
offline contractor capture → delayed sync → revocation/scope drift → review-required claim → dispute/correction → invalidated materialization → blocked DocumentAssembly preview
```

It is intended for conformance and negative testing. It does not create default law for offline sync or farm-owner capture profiles.

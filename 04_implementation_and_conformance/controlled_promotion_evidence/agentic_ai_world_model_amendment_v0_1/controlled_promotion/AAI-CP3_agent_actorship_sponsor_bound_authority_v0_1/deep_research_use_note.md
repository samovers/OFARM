# AAI-CP3 Deep Research use note

The attached Deep Research report was used as supporting context for CP3.

Relevant findings applied:

- promote sponsor-bound actorship after targeted revision;
- require explicit sponsor, executing agent, actorship basis, and authority snapshot;
- prevent semantic laundering where a manifest, handoff, model, tool, prompt, or successful tool call is treated as authority or governance success;
- keep world-model, EvidenceNeed, ObservationRequest, and farmer-facing workflow semantics in experiment-first or later phases.

The report is supporting research only and does not override active baseline law.

## CP3 quality patch — authority-action posture map

Added `03_machine_contracts/maps/authority/OFARM_Agent_Authority_Action_Class_Posture_Map_v0_1.json` so every software-agent AuthorityActionClass posture is explicitly declared rather than left as prose-only guidance.

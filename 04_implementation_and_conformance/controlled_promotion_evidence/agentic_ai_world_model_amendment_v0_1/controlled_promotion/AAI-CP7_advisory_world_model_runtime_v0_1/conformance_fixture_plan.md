# CP7 conformance fixture plan

Positive fixture:

- allow advisory disease-risk scenario only when the run/state/result are Advisory Twin, uncertainty-qualified, validity-windowed, invalidation-aware, output-dispositioned, and result-qualified.

Negative fixtures:

- block world-model state as current-state materialization without accepted bridge;
- block world-model run targeting Compliance Twin;
- block advisory scenario result as compliance basis;
- require review/qualification when invalidated or unqualified world-model output is shown on a public surface.

Required evidence:

- schema-shaped examples;
- trace summary;
- result qualification link;
- governance blocker link;
- no runtime/world-model readiness claim.

# Non-active schema copy index

Active-schema same-basename copies visible under `04_implementation_and_conformance/`: **0**.

Renamed non-active schema-copy files: **77**.

The renamed files use the suffix `__non_active_copy.json` to prevent filename search from treating them as current active schemas.

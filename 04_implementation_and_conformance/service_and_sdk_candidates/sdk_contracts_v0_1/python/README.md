# Python SDK shape candidate

This is a shape contract, not production SDK code.

The public Python SDK should expose only operation-centric clients generated from the public contract pack and should preserve RuntimeProblem codes, trace refs, and result qualification.

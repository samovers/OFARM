// Forbidden OFARM SDK patterns. These examples must not compile in a generated public SDK.

// db.current_state.update({ fieldId: "field:7", status: "treated" });
// ofarm.internal.materializationStore.write(...);
// app.markAsAcceptedExecution("contractor-claim-1");
// aiOutput.promoteToComplianceFact(...);
// client.genericCrud.save("canonicalAssertion", payload);
// client.packs.mergeCoreMeaning(...);
